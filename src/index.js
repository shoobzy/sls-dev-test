import React from 'react';
import { render } from 'react-dom';
import './style.css';

const rootElement = document.getElementById('root');

render(<div className="text-indianred-600 text-center text-5xl">Hello World!</div>, rootElement);
